describe('Clusters', () => {
  it(`Given a large set of points,
  When the map is zoomed out,
  Then points are collected into clusters`, () => {
  });

  it(`Given a set of clusters,
  When the map is zoomed in,
  Cluster size is reduced`, () => {
  });
});
